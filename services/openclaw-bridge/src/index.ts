const message = [
  "US Claw OpenClaw Bridge skeleton initialized.",
  "Future tasks will add install checks, workspace sync, runtime health, and event aggregation."
].join(" ");

console.log(message);
